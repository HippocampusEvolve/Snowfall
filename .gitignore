node_modules/
textures-unused/
dist/
# сборка веб-ассетов под APK (Capacitor копирует её в android/)
dist-apk/
.playwright-mcp/
# временное: скриншоты проверок, разовые скрипты, черновики
temp/*
!temp/README.md
# скриншоты и логи тестов в корне (текстуры в public/ не задевать)
/*.jpeg
/*.jpg
/*.png
/*.gif
*.log
# внутренние заметки — не для публичного репозитория
CLAUDE1.md
CLAUDE.md
.claude/
# сюжетный слой: история и разгадка мира живут в приватном репозитории
# витрины и подкладываются сюда только на время сборки (ops/story.py)
src/story/
# локальные окружения/секреты — на будущее, чтобы не утекли случайно
.env
.env.*
# реквизиты деплоя (root@IP, путь к ключу)
deploy/LOCAL.md
# собранные APK — публикуются в GitHub Releases, а не в git
*.apk
# ключ подписи APK и пароли к нему — НИКОГДА в репозиторий
*.jks
*.keystore
android/keystore.properties
android/key.properties
