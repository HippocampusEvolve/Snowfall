// Пустой Worker для Node.
//
// Проверки счётом читают числа прямо из модулей игры, а те тянут за собой
// matsets.js, который на первой же строке заводит воркеры выпечки материалов.
// В браузере это правильно, на Node - нечего заводить и незачем: карты
// проверке не нужны, нужны числа. Заглушка отдельным модулем, а не строчкой в
// самой проверке, потому что импорты в ESM считаются раньше тела файла:
// присваивание внутри проверки опоздало бы.
globalThis.Worker ??= class {
  postMessage() {}
  terminate() {}
};
